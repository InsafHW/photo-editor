import React from 'react'

import classes from "./ImportPhotoFromPixels.module.css"

const importPhotoFromPixels = (props: any) => {
  return (
    <button className={classes.Element}>Import from Pixels</button>
  )
}

export default importPhotoFromPixels